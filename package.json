{
  "name": "ts-reconf",
  "version": "0.0.1",
  "description": "Explain and simplify (re-conf) your tsconfig.json",
  "main": "index.js",
  "type": "module",
  "bin": {
    "ts-reconf": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "clean": "rm -rf dist",
    "start": "node dist/cli.js",
    "test": "echo \"Todo: write tests\" && exit 1",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "typescript",
    "tsconfig",
    "cli",
    "developer-tools"
  ],
  "author": "rockcoder",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
